# Required: NextAuth secret for session encryption
# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Required: Domain for Caddy HTTPS (production only)
# Example: tracker.yourdomain.com
DOMAIN=

# Optional: SQLite database path inside container (default shown)
DATABASE_PATH=/app/data/tracker.db

# Optional: Seed an admin user on first run
ADMIN_EMAIL=
ADMIN_PASSWORD=
