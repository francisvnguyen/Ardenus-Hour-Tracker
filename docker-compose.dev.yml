services:
  tracker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - AUTH_SECRET=${AUTH_SECRET}
      - DATABASE_PATH=/app/data/tracker.db
    volumes:
      - tracker-data:/app/data
    restart: unless-stopped

volumes:
  tracker-data:
